load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "transpiler",
    srcs = ["transpiler.rs"],
    deps = [
        "@crates//:oxc",
    ],
)

package(default_visibility = ["//visibility:public"])
